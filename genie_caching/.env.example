# =============================================================================
# Demo Notebook Environment Variables
# =============================================================================
# Copy to /Workspace/Users/<your-email>/.env and fill in your values.
# The notebook loads this via dotenv.
# =============================================================================

# App
APP_HOST        = https://<your-app-name>.aws.databricksapps.com
GATEWAY_ID      = <your-gateway-id>

# Genie
GENIE_SPACE_ID  = <your-genie-space-id>

# Auth — OAuth token from Databricks SDK
# In notebooks: TOKEN = WorkspaceClient().config.token
# Locally: databricks auth token --profile <profile>
